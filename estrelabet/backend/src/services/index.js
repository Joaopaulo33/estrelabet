const collaboratorService = require('./collaborator.service');
const companyService = require('./company.service');

/**
 * Os serviços são responsáveis por fazer a manipulação dos modelos do mongoose
 */
module.exports = {
  collaboratorService,
  companyService,
};
